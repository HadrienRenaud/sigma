image: node:latest

cache:
  paths:
  - node_modules/

# Build stages

stages:
  - install
  - build

### Jobs ###

# Install dependencies, compile the bundle.js
install-dependencies:
  stage: install
  script:
    - npm install -q
  artifacts:
    paths:
      - semantic/
      - node_modules/

build:
  stage: build
  script:
    - webpack -v
    - npm run build
  artifacts:
    paths:
      - build/
