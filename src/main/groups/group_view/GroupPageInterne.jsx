import React from 'react';
import { Route, Link } from 'react-router-dom';

import { Button, Segment, Icon, Divider, Card } from 'semantic-ui-react';

const GroupPageInterne = ({ match }) => (
    <div>
        Group PageInterne
        TODO
    </div>
);

export default GroupPageInterne;